#!/usr/bin/env node

const { generate } = require('../src/generate')

generate()
  .then(file => console.log(`Generated new migration file to \x1b[32m"%s"\x1b[0m`, file))
  .catch(err => console.log('\x1b[31m%s\x1b[0m', err))
